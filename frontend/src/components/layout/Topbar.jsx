import {Menu,Sun,Moon,LogOut,Bell} from 'lucide-react';import {useTheme} from '../../context/ThemeContext.jsx';import {useAuth} from '../../context/AuthContext.jsx';import {useNavigate} from 'react-router-dom';
export default function Topbar({onMenu}){const {dark,setDark}=useTheme();const {user,logout}=useAuth();const nav=useNavigate();return <header className="topbar h-16 bg-white border-b border-slate-200 flex items-center justify-between px-4 sm:px-6 sticky top-0 z-30"><div className="flex items-center gap-3"><button className="btn btn-secondary !p-2 lg:hidden" onClick={onMenu}><Menu/></button><div className="hidden sm:block"><p className="text-sm font-bold">Welcome back, {user?.fullName?.split(' ')[0]}</p><p className="text-xs text-slate-400">Keep your inventory under control.</p></div></div><div className="flex items-center gap-1 sm:gap-2"><button className="btn btn-secondary !p-2" onClick={()=>nav('/alerts')} title="Alerts"><Bell size={18}/></button><button className="btn btn-secondary !p-2" onClick={()=>setDark(!dark)} title="Toggle theme">{dark?<Sun size={18}/>:<Moon size={18}/>}</button><button className="btn btn-secondary !p-2" onClick={()=>nav('/profile')} title="Profile"><span className="w-7 h-7 rounded-full bg-blue-100 text-blue-700 grid place-items-center text-xs font-bold">{user?.fullName?.slice(0,1).toUpperCase()}</span></button><button className="btn btn-danger !p-2" onClick={logout} title="Logout"><LogOut size={18}/></button></div></header>}
