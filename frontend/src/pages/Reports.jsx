import {useEffect,useState} from 'react';import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip,PieChart,Pie,Cell} from 'recharts';import api from '../services/api.js';import PageHeader from '../components/common/PageHeader.jsx';import {money} from '../utils/format.js';export default function Reports(){const [sales,setSales]=useState(null),[inventory,setInventory]=useState(null);useEffect(()=>{Promise.all([api.get('/reports/sales?days=30'),api.get('/reports/inventory')]).then(([a,b])=>{setSales(a.data.data);setInventory(b.data.data)})},[]);if(!sales||!inventory)return <div>Loading reports…</div>;return <><PageHeader title="Reports" description="Sales and inventory insights for business decisions."/><div className="grid xl:grid-cols-2 gap-4"><div className="surface p-5"><h2 className="font-bold">Sales — last 30 days</h2><div className="h-72 mt-4"><ResponsiveContainer width="100%" height="100%"><BarChart data={sales.daily}><XAxis dataKey="_id"/><YAxis/><Tooltip formatter={v=>money(v)}/><Bar dataKey="sales" fill="#173b72" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></div><div className="surface p-5"><h2 className="font-bold">Inventory by category</h2><div className="h-72 mt-4"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={inventory.inventory} dataKey="value" nameKey="_id" outerRadius={95} label>{inventory.inventory.map((x,i)=><Cell key={i} fill={['#173b72','#22c55e','#f59e0b','#ef4444','#8b5cf6'][i%5]}/>)}</Pie><Tooltip formatter={v=>money(v)}/></PieChart></ResponsiveContainer></div></div></div><div className="surface p-5 mt-4"><h2 className="font-bold">Top-selling products</h2><div className="table-wrap mt-4"><table className="w-full text-sm"><thead className="text-left text-slate-500 border-b"><tr><th className="py-3">Product</th><th>Quantity sold</th><th>Revenue</th></tr></thead><tbody>{sales.topProducts.map(x=><tr className="border-b" key={x._id}><td className="py-3 font-semibold">{x.name}</td><td>{x.quantity}</td><td>{money(x.revenue)}</td></tr>)}</tbody></table></div></div></>}
