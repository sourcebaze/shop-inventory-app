import {useState} from 'react';import {useAuth} from '../context/AuthContext.jsx';import api from '../services/api.js';import PageHeader from '../components/common/PageHeader.jsx';import {getError} from '../utils/format.js';export default function Profile(){const {user}=useAuth();const [form,setForm]=useState({fullName:user.fullName,email:user.email,password:''}),[msg,setMsg]=useState(''),[error,setError]=useState('');const save=async e=>{e.preventDefault();try{const r=await api.put('/profile',{...form,password:form.password||undefined});localStorage.setItem('stockwise_user',JSON.stringify(r.data.data.user));setMsg('Profile updated. Refresh if you want the top bar name to update.');setError('')}catch(err){setError(getError(err))}};return <><PageHeader title="Profile" description="Update your account information."/><form onSubmit={save} className="surface p-5 max-w-xl space-y-4">{msg&&<div className="p-3 bg-green-50 text-green-700 rounded-lg text-sm">{msg}</div>}{error&&<div className="p-3 bg-red-50 text-red-700 rounded-lg text-sm">{error}</div>}<div><label className="label">Full name</label><input className="input" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></div><div><label className="label">Email</label><input className="input" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div><label className="label">New password</label><input className="input" type="password" minLength="6" placeholder="Leave blank to keep current password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><button className="btn btn-primary">Save Profile</button></form></>}
